---
phase: 04-relat-rios-e-refinamentos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ReportsDashboard.tsx
  - src/tests/ReportsDashboard.test.tsx
autonomous: true
requirements:
  - R4
must_haves:
  truths:
    - "Ao abrir a tela de Relatórios, o dashboard busca dados reais via relatoriosApi.upa(usuario.upa_id)"
    - "Os três cards (Médicos Ativos, Total Horas, Taxa de Disponibilidade) exibem valores vindos do payload da API, não constantes hardcoded"
    - "A tabela 'Detalhamento por Profissional' lista os médicos retornados em detalhamento[] da API"
    - "O filtro de mês é um <input type='month'> funcional; mudar o valor re-executa o fetch com ?mes=YYYY-MM"
    - "Indicador de loading aparece durante o primeiro fetch e desaparece quando os dados chegam"
    - "Gestor municipal acessa a mesma tela de Relatórios via navegação 'reports' (payload municipal continua sendo exibido no MunicipalManagerDashboard sem alteração — conforme research)"
  artifacts:
    - path: "src/components/ReportsDashboard.tsx"
      provides: "Componente conectado a relatoriosApi.upa() com filtro de mês"
      contains: "relatoriosApi.upa"
    - path: "src/tests/ReportsDashboard.test.tsx"
      provides: "Testes unitários de integração com API + filtro de mês"
      contains: "relatoriosApi"
  key_links:
    - from: "src/components/ReportsDashboard.tsx"
      to: "relatoriosApi.upa"
      via: "useEffect + useCallback no mount e ao mudar mesSelecionado"
      pattern: "relatoriosApi\\.upa\\("
    - from: "src/components/ReportsDashboard.tsx"
      to: "useApp().usuario.upa_id"
      via: "hook useApp importado de ../store/AppContext"
      pattern: "useApp\\(\\)"
---

<objective>
Conectar `ReportsDashboard.tsx` ao endpoint real `/api/relatorios/upa/<id>/` substituindo dados mock por estado dinâmico, implementar filtro de mês funcional com `<input type="month">`, e criar testes unitários cobrindo o comportamento.

Purpose: Atender R4 — o relatório de UPA deve exibir dados reais (médicos ativos, total de horas, taxa de disponibilidade, detalhamento por profissional) com filtro de mês, que hoje usa constantes hardcoded.

Output:
- `ReportsDashboard.tsx` refatorado (mantém layout, gráfico decorativo e header existentes)
- `ReportsDashboard.test.tsx` novo com 3 testes (mount fetch, mudança de mês, loading)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-relat-rios-e-refinamentos/04-RESEARCH.md

@src/components/ReportsDashboard.tsx
@src/components/UPAManagerDashboard.tsx
@src/services/api.ts
@src/store/AppContext.tsx
@src/tests/UPAManagerDashboard.test.tsx

<interfaces>
<!-- Contratos extraídos do codebase. Usar diretamente sem explorar. -->

From src/services/api.ts:
```typescript
export const relatoriosApi = {
  upa: (upa_id: number | string, mes?: string) => Promise<any>,
  municipio: (municipio_id: number | string) => Promise<any>,
};
```

Resposta de `relatoriosApi.upa(upa_id, mes?)` [VERIFIED: views.py]:
```json
{
  "upa": { "id": 1, "nome": "UPA 24h Norte", "bairro": "Itapuã" },
  "mes": "2026-04",
  "medicos_ativos": 8,
  "total_horas": "320h",
  "taxa_disponibilidade": "87%",
  "detalhamento": [
    { "id": 3, "nome": "Dr. Carlos Lima", "especialidade": "Clínico Geral",
      "total_horas": "42h", "assiduidade": "89%", "status": "Online" }
  ]
}
```

From src/store/AppContext.tsx:
```typescript
export function useApp(): { usuario?: { upa_id?: number; municipio_id?: number; ... }; ... }
```

Props do componente (manter assinatura):
```typescript
interface ReportsDashboardProps {
  onBack: () => void;
  userName: string;
  onLogout: () => void;
}
```

Formato do filtro: `<input type="month">` retorna `"YYYY-MM"` nativamente — passar direto para a API.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar testes unitários de ReportsDashboard (RED)</name>
  <files>src/tests/ReportsDashboard.test.tsx</files>
  <behavior>
    - Test 1: no mount, `relatoriosApi.upa` é chamado com `usuario.upa_id` (mock = 42)
    - Test 2: após mudar `<input type="month">` para "2026-04", `relatoriosApi.upa` é chamado com `(42, "2026-04")`
    - Test 3: `isLoading` inicial mostra texto/indicador de carregamento; após fetch os dados da API (ex: `medicos_ativos: 8`) aparecem na tela
  </behavior>
  <action>
    Criar `src/tests/ReportsDashboard.test.tsx` seguindo o padrão de `src/tests/UPAManagerDashboard.test.tsx`:
    - Mock `../services/api` com `relatoriosApi: { upa: vi.fn().mockResolvedValue({ medicos_ativos: 8, total_horas: "320h", taxa_disponibilidade: "87%", detalhamento: [{ id: 1, nome: "Dr. X", especialidade: "Clínico", total_horas: "10h", assiduidade: "90%", status: "Online" }] }) }`
    - Mock `../store/AppContext` com `useApp: () => ({ usuario: { upa_id: 42 } })`
    - Teste 1: renderizar, esperar `act(async () => await Promise.resolve())`, verificar `relatoriosApi.upa` chamado com `42` (ou `42, undefined`)
    - Teste 2: renderizar, aguardar primeiro fetch, usar `fireEvent.change` no input `type="month"` com value "2026-04", aguardar, verificar última chamada teve `mes="2026-04"`
    - Teste 3: renderizar; antes do resolve, algum elemento de loading visível; após resolve, `screen.getByText("8")` (medicos_ativos) visível; `screen.getByText(/Dr\. X/)` presente
    - Props de teste: `{ userName: 'Gestor', onBack: vi.fn(), onLogout: vi.fn() }`
    - Rodar `npm test -- --run ReportsDashboard` — testes DEVEM falhar (RED — componente ainda usa mock)
    - Commit: `test(04-01): add failing tests for ReportsDashboard API integration`
  </action>
  <verify>
    <automated>npm test -- --run ReportsDashboard.test.tsx</automated>
    Esperado: 3 testes executam e falham (componente ainda hardcoded). Sem erro de import/sintaxe.
  </verify>
  <done>Arquivo existe, 3 testes escritos, execução falha com mensagens relacionadas a API não chamada ou dados mock ainda visíveis.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Conectar ReportsDashboard ao relatoriosApi.upa (GREEN)</name>
  <files>src/components/ReportsDashboard.tsx</files>
  <behavior>
    - Após mount: `relatoriosApi.upa(usuario.upa_id, mesSelecionado || undefined)` é chamado
    - Cards exibem `dados.medicos_ativos`, `dados.total_horas`, `dados.taxa_disponibilidade`
    - Tabela de detalhamento itera sobre `dados.detalhamento` (cada item: nome, especialidade, total_horas, assiduidade, status)
    - `<input type="month">` controlado por `mesSelecionado`; onChange re-dispara fetch via dep do useCallback
    - `isLoading` inicial true; `setIsLoading(false)` apenas no `.finally()` do primeiro fetch (padrão UPAManagerDashboard)
  </behavior>
  <action>
    Refatorar `src/components/ReportsDashboard.tsx`:

    1. Adicionar imports: `useState, useEffect, useCallback` de `'react'`, `relatoriosApi` de `'../services/api'`, `useApp` de `'../store/AppContext'`, `Loader2` de `'lucide-react'`.

    2. Remover a constante `MOCK_DOCTOR_STATS`.

    3. Dentro do componente:
    ```tsx
    const { usuario } = useApp();
    const [dados, setDados] = useState<any>(null);
    const [mesSelecionado, setMesSelecionado] = useState<string>('');
    const [isLoading, setIsLoading] = useState(true);

    const fetchDados = useCallback(async () => {
      const upaId = usuario?.upa_id ?? usuario?.municipio_id; // fallback removido — ver nota
      if (!upaId) return;
      try {
        const result = await relatoriosApi.upa(upaId, mesSelecionado || undefined);
        setDados(result);
      } catch {}
    }, [usuario?.upa_id, mesSelecionado]);

    useEffect(() => {
      fetchDados().finally(() => setIsLoading(false));
    }, [fetchDados]);
    ```

    **IMPORTANTE sobre `upa_id`:** `ReportsDashboard` é acessado por gestor de UPA (tem `upa_id`) E gestor municipal (via rota `'reports'`). Para o gestor municipal a tela mostra o relatório de UPA 1 de exemplo; nesta fase **mantenha apenas `usuario?.upa_id`** — se não existir, o componente apenas mostra "Sem UPA selecionada". Gestor municipal continua vendo seu dashboard próprio (MunicipalManagerDashboard) que já mostra dados do município. NÃO adicionar fallback para municipio_id.

    Código correto:
    ```tsx
    const upaId = usuario?.upa_id;
    if (!upaId) return;
    ```

    4. Substituir o bloco "Date Filter":
    ```tsx
    <div className="bg-white p-4 rounded-2xl border border-slate-100 shadow-sm flex items-center gap-3">
      <Calendar size={20} className="text-slate-600" />
      <label className="text-sm font-bold text-slate-600">Filtrar por mês:</label>
      <input
        type="month"
        value={mesSelecionado}
        onChange={e => setMesSelecionado(e.target.value)}
        className="px-3 py-1 border border-slate-200 rounded-lg text-sm"
      />
      {mesSelecionado && (
        <button
          onClick={() => setMesSelecionado('')}
          className="text-blue-600 text-xs font-bold hover:underline ml-auto"
        >Limpar</button>
      )}
    </div>
    ```

    5. Substituir os três cards: valores `24`, `3.840h`, `92%` → `dados?.medicos_ativos ?? '-'`, `dados?.total_horas ?? '-'`, `dados?.taxa_disponibilidade ?? '-'`. Remover os textos secundários hardcoded ("Todos com escalas em dia", "+4% vs mês anterior", "Média de 8h/dia por médico") ou substituí-los por texto estático genérico (ex: "Baseado em turnos registrados").

    6. Renderização da lista: substituir `MOCK_DOCTOR_STATS.map((doc) => ...)` por `(dados?.detalhamento ?? []).map((doc: any) => ...)` trocando as chaves:
       - `doc.name` → `doc.nome`
       - `doc.specialty` → `doc.especialidade`
       - `doc.totalHours` → `doc.total_horas`
       - `doc.availability` → `doc.assiduidade`
       - `doc.status` mantém-se
       - key: `doc.id`

    7. Loading state: acima do grid de cards, condicional:
    ```tsx
    {isLoading && (
      <div className="flex justify-center items-center py-8">
        <Loader2 className="animate-spin text-slate-400" size={24} />
        <span className="ml-2 text-sm text-slate-500">Carregando...</span>
      </div>
    )}
    ```

    8. Empty state na tabela: se `!isLoading && (!dados?.detalhamento || dados.detalhamento.length === 0)` exibir `<p className="text-center text-slate-400 text-sm py-4">Nenhum médico no período selecionado.</p>`.

    9. **MANTER** o gráfico de barras decorativo (alturas `[60, 75, ...]`) — research assumption A1 confirma que permanece decorativo.

    10. **MANTER** o botão "Exportar" atualmente sem handler — fora de escopo (não é requisito R4).

    11. Rodar `npm test -- --run ReportsDashboard.test.tsx` — todos os 3 testes devem PASSAR.
    12. Rodar full suite: `npm test -- --run` — nenhum teste existente deve quebrar.
    13. Commit: `feat(04-01): connect ReportsDashboard to real API with month filter`
  </action>
  <verify>
    <automated>npm test -- --run ReportsDashboard.test.tsx &amp;&amp; npm test -- --run</automated>
    Esperado: 3 testes de ReportsDashboard passam + toda a suíte existente continua verde.
  </verify>
  <done>
    - `relatoriosApi.upa` importado e chamado no componente
    - Cards mostram valores de `dados`, não hardcoded
    - Tabela itera sobre `dados.detalhamento` com chaves em português
    - Input `type="month"` funcional, muda filtro e re-fetch
    - Nenhuma referência a `MOCK_DOCTOR_STATS` restante
    - Todos os testes passam
  </done>
</task>

</tasks>

<verification>
- `npm test -- --run ReportsDashboard.test.tsx` → 3/3 passam
- `npm test -- --run` → suíte completa verde (incluindo testes existentes de CitizenPortal, DoctorDashboard, UPAManagerDashboard)
- Grep: `grep -n "MOCK_DOCTOR_STATS" src/components/ReportsDashboard.tsx` → vazio
- Grep: `grep -n "relatoriosApi.upa" src/components/ReportsDashboard.tsx` → 1 match
</verification>

<success_criteria>
1. `ReportsDashboard` busca dados reais via `relatoriosApi.upa(usuario.upa_id)` no mount
2. Filtro `<input type="month">` re-executa fetch com `?mes=YYYY-MM`
3. Cards e tabela de detalhamento renderizam dados do payload (campos em português)
4. Loading visível durante primeiro fetch; desaparece ao carregar
5. Testes automatizados (3) passam; suíte existente permanece verde
6. Gráfico decorativo e "Exportar" mantidos sem alteração (fora de R4)
</success_criteria>

<output>
Após completar, criar `.planning/phases/04-relat-rios-e-refinamentos/04-01-SUMMARY.md` com:
- Arquivos modificados e linhas-chave
- Status de testes (3/3 ReportsDashboard + suite total)
- Decisão registrada: `upa_id` vem apenas de `usuario?.upa_id` (sem fallback para município)
- Screenshot/descrição visual se possível (componente antes/depois)
</output>
</content>
</invoke>