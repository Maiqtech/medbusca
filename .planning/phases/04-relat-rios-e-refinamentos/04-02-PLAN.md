---
phase: 04-relat-rios-e-refinamentos
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/App.tsx
  - src/components/ReportsDashboard.tsx
  - src/components/MunicipalManagerDashboard.tsx
  - src/components/DoctorDashboard.tsx
  - src/components/CitizenPortal.tsx
  - src/components/SuperAdminDashboard.tsx
  - src/components/UPAManagerDashboard.tsx
autonomous: false
requirements:
  - R4
must_haves:
  truths:
    - "Todos os 5 perfis (Cidadão, Médico, Gestor de UPA, Gestor Municipal, Super Admin) completam seus fluxos principais sem erro JavaScript no console"
    - "Navegação 'reports' funciona tanto para gestor de UPA quanto para gestor municipal (com botão voltar correto em cada caso)"
    - "Nenhuma referência morta a `alertasApi` permanece em componentes de dashboard (removida em commit bc24ec4)"
    - "Bugs encontrados durante QA são registrados e corrigidos ou documentados como fora-de-escopo"
    - "Checklist UAT dos 5 perfis executado e aprovado pelo usuário"
  artifacts:
    - path: ".planning/phases/04-relat-rios-e-refinamentos/04-UAT.md"
      provides: "Registro de teste manual dos 5 perfis com resultados"
      contains: "Perfil"
  key_links:
    - from: "src/App.tsx rota 'reports'"
      to: "ReportsDashboard.onBack"
      via: "onBack deve retornar para o dashboard correto (upa_manager_dashboard OU municipal_manager_dashboard)"
      pattern: "currentView === 'reports'"
---

<objective>
Revisão final de qualidade (QA) dos 5 perfis do sistema MedBusca antes da entrega do TCC. Executar os fluxos principais de cada perfil, identificar bugs e referências mortas, corrigir problemas encontrados, e documentar o resultado em UAT.md.

Purpose: Atender o terceiro critério de sucesso de R4 — "Todos os 5 perfis completam seus fluxos principais sem erro". Garantir que o sistema está apto para demonstração ao banco examinador.

Output:
- Bugs corrigidos (escopo: fluxos que quebram, não redesigns)
- Referências mortas removidas (ex: sobras de `alertasApi`)
- Ajuste da navegação `reports` para funcionar com ambos os gestores
- `04-UAT.md` documentando o resultado dos 5 fluxos
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-relat-rios-e-refinamentos/04-RESEARCH.md
@.planning/phases/04-relat-rios-e-refinamentos/04-01-PLAN.md

@src/App.tsx
@src/components/ReportsDashboard.tsx
@src/components/MunicipalManagerDashboard.tsx
@src/components/UPAManagerDashboard.tsx
@src/components/DoctorDashboard.tsx
@src/components/CitizenPortal.tsx
@src/components/SuperAdminDashboard.tsx

<interfaces>
<!-- Research identificou áreas de risco específicas: -->

Risco 1 — `alertasApi` removido em commit bc24ec4 (mas apenas de UPAManagerDashboard; verificar se sobrou em outros componentes):
```bash
grep -rn "alertasApi" src/components/
```

Risco 2 — Rota 'reports' em App.tsx:
Atual: `onBack={() => setCurrentView('upa_manager_dashboard')}`
Problema: gestor municipal que clica em "Relatórios" volta para dashboard de UPA errado.
Solução: `onBack={() => setCurrentView(usuario?.perfil === 'gestor_municipal' ? 'municipal_manager_dashboard' : 'upa_manager_dashboard')}`

Risco 3 — MOCK_MUNICIPALITIES em App.tsx com logos externos (CORS/404):
```bash
grep -n "MOCK_MUNICIPALITIES" src/App.tsx
```

Os 5 perfis e fluxos [VERIFIED: models.py + App.tsx]:
| Perfil | Credencial teste | Fluxo |
|--------|------------------|-------|
| Cidadão | sem login | Portal → buscar UPA → ver resultados |
| Médico | perfil=medico | Login → DoctorDashboard → iniciar/pausar/encerrar turno |
| Gestor UPA | perfil=gestor_upa | Login → UPAManagerDashboard → ver médicos → Relatórios → voltar |
| Gestor Municipal | perfil=gestor_municipal | Login → MunicipalManagerDashboard → stats → Relatórios → voltar |
| Super Admin | perfil=super_admin | Login → SuperAdminDashboard → cadastrar município/gestor |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar e corrigir referências mortas + navegação 'reports'</name>
  <files>
    src/App.tsx
    src/components/ReportsDashboard.tsx
    src/components/DoctorDashboard.tsx
    src/components/CitizenPortal.tsx
    src/components/SuperAdminDashboard.tsx
    src/components/MunicipalManagerDashboard.tsx
    src/components/UPAManagerDashboard.tsx
  </files>
  <action>
    **1. Auditar referências mortas:**
    ```bash
    grep -rn "alertasApi" src/components/
    grep -rn "MOCK_DOCTOR_STATS\|MOCK_MEDICOS\|MOCK_TURNOS" src/components/
    grep -rn "console.log\|console.error" src/components/ | grep -v "test"
    ```

    Para cada match encontrado:
    - Referência a `alertasApi` em componentes de dashboard: **remover** import e chamada (endpoint deletado em commit f835203)
    - Constantes MOCK_* não usadas: **remover**
    - `console.log`/`console.error` de debug (não erros legítimos): **remover**

    Executar `npm run build` (ou `npx tsc --noEmit`) após cada remoção — TypeScript deve permanecer verde.

    **2. Corrigir navegação 'reports' em App.tsx:**

    Localizar a renderização de `ReportsDashboard` (research indica `App.tsx:337`):
    ```tsx
    case 'reports':
      return <ReportsDashboard
        userName={usuario?.nome ?? ''}
        onLogout={...}
        onBack={() => setCurrentView('upa_manager_dashboard')}
      />;
    ```

    Substituir `onBack` por navegação condicional:
    ```tsx
    onBack={() => setCurrentView(
      usuario?.perfil === 'gestor_municipal' ? 'municipal_manager_dashboard' : 'upa_manager_dashboard'
    )}
    ```

    Se a estrutura do switch for diferente, adaptar preservando a lógica. Se `usuario?.perfil` não existir no tipo, usar comparação equivalente (ex: checar se `usuario?.municipio_id && !usuario?.upa_id`).

    **3. Auditar MOCK_MUNICIPALITIES em App.tsx:**
    ```bash
    grep -n "MOCK_MUNICIPALITIES\|logo" src/App.tsx
    ```
    Se houver URLs externas (ex: `https://...`/`http://...`) em `logo` fields: substituir por `undefined` ou por emoji/ícone interno (ex: `'🏛️'`). Motivo: evitar falha visual por CORS/404 durante apresentação TCC.

    **4. Verificar imports não usados em todos os 7 arquivos listados:**
    ```bash
    npx tsc --noEmit
    ```
    Corrigir quaisquer warnings TS6133 (imported but never used).

    **5. Executar suíte de testes:**
    ```bash
    npm test -- --run
    ```
    Todos os testes existentes devem continuar verdes.

    **6. Build de produção:**
    ```bash
    npm run build
    ```
    Deve compilar sem erros.

    Commit: `fix(04-02): remove dead references and fix reports navigation for municipal manager`
  </action>
  <verify>
    <automated>npm test -- --run &amp;&amp; npm run build</automated>
    Esperado: todos os testes passam, build Vite conclui sem erros.
  </verify>
  <done>
    - `grep -rn "alertasApi" src/components/` retorna apenas referências legítimas (nenhuma em dashboards de médico/UPA/município)
    - `App.tsx` rota 'reports' tem onBack condicional por perfil
    - MOCK_MUNICIPALITIES não tem URLs externas quebráveis
    - `npx tsc --noEmit` limpo
    - `npm test -- --run` e `npm run build` verdes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT dos 5 perfis — execução manual e registro em 04-UAT.md</name>
  <what-built>
    Plano 04-01 concluído (ReportsDashboard conectado à API real) + Task 1 desta fase (referências mortas removidas, navegação 'reports' corrigida).

    Você deve agora executar os 5 fluxos principais e documentar o resultado.

    Claude deve **criar o arquivo** `.planning/phases/04-relat-rios-e-refinamentos/04-UAT.md` com o template abaixo preenchido com campos `[PENDENTE]` para o usuário marcar durante o teste, e iniciar os servidores local:

    ```bash
    # Backend (Django)
    cd backend &amp;&amp; python manage.py runserver 8000 &amp;
    # Frontend (Vite)
    npm run dev
    ```

    Template de `04-UAT.md`:

    ```markdown
    # Fase 4 — UAT dos 5 Perfis

    **Data:** {preencher}
    **Ambiente:** local (Django 8000 + Vite 5173)
    **Testador:** Antonio Maia

    ## Checklist por Perfil

    ### 1. Cidadão (sem login)
    - [ ] Portal abre na landing page
    - [ ] Busca de UPA por município retorna lista
    - [ ] Status das UPAs atualiza a cada 10s (polling Fase 3)
    - [ ] Nenhum erro no DevTools console
    - **Bugs:** {nenhum | descrever}

    ### 2. Médico
    - [ ] Login com credencial de médico funciona
    - [ ] DoctorDashboard mostra turno do dia
    - [ ] Iniciar turno → status muda para "Em Atendimento"
    - [ ] Pausar turno → status muda para "Pausado"
    - [ ] Encerrar turno → turno some / marcado como concluído
    - [ ] Polling 15s atualiza status automaticamente
    - **Bugs:** {nenhum | descrever}

    ### 3. Gestor de UPA
    - [ ] Login com credencial de gestor de UPA funciona
    - [ ] UPAManagerDashboard lista médicos da UPA
    - [ ] Polling 15s atualiza lista
    - [ ] Clicar em "Relatórios" abre ReportsDashboard
    - [ ] ReportsDashboard exibe dados reais (médicos ativos, total horas, taxa, tabela de detalhamento)
    - [ ] Filtro de mês `<input type="month">` re-carrega os dados
    - [ ] Botão voltar retorna para UPAManagerDashboard
    - **Bugs:** {nenhum | descrever}

    ### 4. Gestor Municipal
    - [ ] Login com credencial de gestor municipal funciona
    - [ ] MunicipalManagerDashboard mostra UPAs do município
    - [ ] Stats (UPAs Cadastradas, Unidades Ativas, Médicos Vinculados, Em Atendimento) populados
    - [ ] Clicar em "Relatórios" abre ReportsDashboard
    - [ ] Botão voltar retorna para MunicipalManagerDashboard (não UPA manager)
    - **Bugs:** {nenhum | descrever}

    ### 5. Super Admin
    - [ ] Login com credencial super_admin funciona
    - [ ] SuperAdminDashboard carrega
    - [ ] Cadastrar novo município funciona (formulário + POST)
    - [ ] Cadastrar novo gestor funciona
    - [ ] Listar municípios mostra o recém-criado
    - **Bugs:** {nenhum | descrever}

    ## Resumo

    | Perfil | Status | Bugs |
    |--------|--------|------|
    | Cidadão | {PASS/FAIL} | {count} |
    | Médico | {PASS/FAIL} | {count} |
    | Gestor UPA | {PASS/FAIL} | {count} |
    | Gestor Municipal | {PASS/FAIL} | {count} |
    | Super Admin | {PASS/FAIL} | {count} |

    ## Bugs a corrigir

    1. {descrição + severidade}

    ## Bugs fora de escopo (pós-TCC)

    1. {descrição + justificativa}
    ```
  </what-built>
  <how-to-verify>
    **Passos do testador (Antonio):**

    1. Garantir que backend e frontend estão rodando:
       - Backend: http://localhost:8000 (Django)
       - Frontend: http://localhost:5173 (Vite)

    2. Ter credenciais de teste para cada perfil (verificar fixtures/seeds ou criar via Django admin).

    3. Abrir o DevTools (F12) aba Console — manter aberto durante TODOS os testes para capturar erros JavaScript.

    4. Para cada perfil no checklist:
       - Executar cada item sequencialmente
       - Marcar `[x]` quando passar, deixar `[ ]` quando falhar
       - Se falhar: registrar em "Bugs" com descrição + passo de reprodução
       - Tirar print se o bug for visual

    5. Preencher "Resumo" e "Bugs a corrigir" ao final.

    6. Commit: `test(04-02): UAT 5 perfis — {PASS_COUNT}/5 aprovados`

    7. **Se houver bugs críticos (bloqueantes de fluxo):**
       - Voltar a `resume-signal` com: `"bugs encontrados: <lista>"` → Claude abre nova task para corrigir
       - Após correção, re-executar apenas os fluxos afetados

    8. **Se todos os fluxos passam OU apenas bugs cosméticos restam:**
       - Marcar como aprovado em "Resumo"
       - `resume-signal`: `"approved"` → fase 4 completa
  </how-to-verify>
  <resume-signal>
    Digite uma das opções:
    - `approved` — todos os 5 fluxos passaram (ou bugs cosméticos aceitos como fora-de-escopo)
    - `bugs: <descrição>` — há bugs bloqueantes a corrigir antes da entrega
  </resume-signal>
</task>

</tasks>

<verification>
- Arquivo `04-UAT.md` existe com os 5 perfis testados
- `grep -rn "alertasApi" src/components/` sem matches em dashboards
- `npm test -- --run` e `npm run build` verdes
- Checklist UAT aprovado pelo usuário
</verification>

<success_criteria>
1. Referências mortas (alertasApi em dashboards, MOCK_* não usados, console.log de debug) removidas
2. Navegação 'reports' funciona para ambos gestor UPA e gestor municipal (onBack correto)
3. Os 5 perfis completam seus fluxos principais sem erro no console
4. `04-UAT.md` documenta resultado do teste manual
5. Nenhum teste automatizado quebrou
6. Sistema está apto para demonstração do TCC
</success_criteria>

<output>
Após completar, criar `.planning/phases/04-relat-rios-e-refinamentos/04-02-SUMMARY.md` com:
- Lista de bugs encontrados e corrigidos
- Lista de bugs fora-de-escopo (se houver)
- Confirmação dos 5 fluxos UAT
- Status final do sistema para entrega TCC
</output>
</content>
</invoke>