---
phase: 01-recupera-o-de-senha
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/models.py
  - api/email_service.py
  - api/serializers.py
  - api/views.py
  - api/urls.py
autonomous: true
requirements:
  - R1
must_haves:
  truths:
    - "POST /api/auth/esqueci-senha/ com email válido envia email com link de redefinição"
    - "POST /api/auth/esqueci-senha/ com email inexistente retorna 200 sem revelar ausência"
    - "POST /api/auth/redefinir-senha/ com token válido e nova_senha altera a senha do usuário"
    - "Token expirado (>1h) retorna 400 com mensagem de erro"
    - "Token já usado retorna 400 com mensagem de erro"
    - "Novo pedido de esqueci-senha invalida tokens anteriores do mesmo usuário"
    - "Migration criada para o model TokenRedefinicaoSenha"
  artifacts:
    - path: "api/models.py"
      provides: "Model TokenRedefinicaoSenha"
      contains: "class TokenRedefinicaoSenha"
    - path: "api/email_service.py"
      provides: "Envio de email de redefinição"
      exports: ["enviar_email_redefinicao_senha"]
    - path: "api/views.py"
      provides: "Views esqueci_senha e redefinir_senha"
      exports: ["esqueci_senha", "redefinir_senha"]
    - path: "api/urls.py"
      provides: "Rotas dos novos endpoints"
      contains: "auth/esqueci-senha"
  key_links:
    - from: "api/views.py::esqueci_senha"
      to: "api/email_service.py::enviar_email_redefinicao_senha"
      via: "import direto, chamada com (usuario.nome, usuario.email, token_str)"
    - from: "api/views.py::redefinir_senha"
      to: "api/models.py::TokenRedefinicaoSenha"
      via: "TokenRedefinicaoSenha.objects.get(token=token_str, usado=False)"
---

<objective>
Implementar o fluxo backend de recuperação de senha: model TokenRedefinicaoSenha, email
de redefinição, e dois endpoints autenticados publicamente.

Purpose: Permitir que usuários redefinam sua senha sem intervenção de admin — requisito R1.
Output: Dois endpoints funcionais, model com migration, email HTML enviado via send_mail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Padrões existentes que este plano DEVE seguir. Não explorar o codebase — usar diretamente. -->

De api/models.py — referência para o novo model (usar ForeignKey, não OneToOne):
```python
class TokenAtivacao(models.Model):
    usuario = models.OneToOneField(Usuario, on_delete=models.CASCADE, related_name='token_ativacao')
    token = models.CharField(max_length=64, unique=True)
    criado_em = models.DateTimeField(auto_now_add=True)
    expira_em = models.DateTimeField()
    usado = models.BooleanField(default=False)

    class Meta:
        db_table = 'tokens_ativacao'
```

De api/views.py — padrão de view @api_view AllowAny (replicar exatamente):
```python
@api_view(['POST'])
@permission_classes([AllowAny])
def ativar_conta(request):
    token_str = request.data.get('token')
    senha = request.data.get('senha')
    if not token_str or not senha:
        return Response({'erro': '...'}, status=400)
    try:
        t = TokenAtivacao.objects.get(token=token_str, usado=False)
    except TokenAtivacao.DoesNotExist:
        return Response({'erro': '...'}, status=400)
    if timezone.now() > t.expira_em:
        return Response({'erro': '...'}, status=400)
    t.usuario.set_password(senha)
    t.usuario.save()
    t.usado = True
    t.save()
    return Response({'mensagem': '...'})
```

De api/email_service.py — assinatura padrão (replicar estrutura):
```python
def enviar_email_ativacao(nome: str, email: str, token: str, perfil: str, contexto: str):
    frontend_url = getattr(settings, 'FRONTEND_URL', 'http://localhost:3000')
    link = f'{frontend_url}/?token={token}'
    # ... html ...
    send_mail(subject=..., message=..., from_email=settings.DEFAULT_FROM_EMAIL,
              recipient_list=[email], html_message=html, fail_silently=False)
    return True  # retorna bool, erros logados com print(f'[EMAIL ERROR] {e}')
```

De api/urls.py — padrão de registro:
```python
path('auth/login/', views.MedBuscaTokenObtainPairView.as_view(), name='token_obtain_pair'),
path('ativar/<str:token>/', views.verificar_token_ativacao),
path('ativar/', views.ativar_conta),
```

De api/serializers.py — imports atuais (TokenRedefinicaoSenha precisará ser adicionado):
```python
from .models import TokenAtivacao, Usuario, Municipio, Especialidade, UPA, Medico, Escala, Turno, RegistroTurno, Alerta
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar model TokenRedefinicaoSenha e migration</name>
  <files>api/models.py</files>
  <action>
Adicionar ao final de api/models.py, após a classe TokenAtivacao (linha 189), o novo model.
Usar ForeignKey (não OneToOne) para suportar múltiplos pedidos do mesmo usuário.

```python
class TokenRedefinicaoSenha(models.Model):
    usuario = models.ForeignKey(Usuario, on_delete=models.CASCADE, related_name='tokens_redefinicao')
    token = models.CharField(max_length=64, unique=True)
    criado_em = models.DateTimeField(auto_now_add=True)
    expira_em = models.DateTimeField()
    usado = models.BooleanField(default=False)

    class Meta:
        db_table = 'tokens_redefinicao_senha'

    def __str__(self):
        return f'Redefinição de {self.usuario.email}'
```

Depois, gerar e aplicar a migration:
```bash
python manage.py makemigrations api --name tokens_redefinicao_senha
python manage.py migrate
```

NÃO alterar nenhum model existente. NÃO alterar TokenAtivacao.
  </action>
  <verify>
    <automated>cd /c/Users/anton/Downloads/medbusca && python manage.py showmigrations api | grep tokens_redefinicao_senha</automated>
  </verify>
  <done>Model TokenRedefinicaoSenha existe em api/models.py com ForeignKey para Usuario. Migration aplicada com sucesso (showmigrations mostra [X]).</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar enviar_email_redefinicao_senha ao email_service</name>
  <files>api/email_service.py</files>
  <action>
Adicionar a função abaixo ao final de api/email_service.py, após a função enviar_email_ativacao.
Seguir exatamente o mesmo padrão: link com query param `?reset_token=`, HTML inline com mesma
estrutura e paleta de cores, send_mail com fail_silently=False, retorno bool, erro logado com print.

O link de redefinição aponta para a rota do frontend de reset (a ser criada no plano 01-02).
Usar `?reset_token=` (diferente de `?token=` do fluxo de ativação) para não colidir.

```python
def enviar_email_redefinicao_senha(nome: str, email: str, token: str):
    """Envia email com link para redefinição de senha. Válido por 1 hora."""
    frontend_url = getattr(settings, 'FRONTEND_URL', 'http://localhost:3000')
    link = f'{frontend_url}/?reset_token={token}'

    html = f'''
<!DOCTYPE html>
<html lang="pt-BR">
<head><meta charset="UTF-8"></head>
<body style="margin:0;padding:0;background:#f8fafc;font-family:sans-serif;">
  <table width="100%" cellpadding="0" cellspacing="0" style="background:#f8fafc;padding:40px 20px;">
    <tr><td align="center">
      <table width="560" cellpadding="0" cellspacing="0" style="background:#fff;border-radius:24px;overflow:hidden;box-shadow:0 4px 24px rgba(0,0,0,0.06);">
        <tr>
          <td style="background:#2563eb;padding:32px 40px;">
            <h1 style="margin:0;color:#fff;font-size:24px;font-weight:900;letter-spacing:-0.5px;">
              Med<span style="opacity:0.7;">Busca</span>
            </h1>
            <p style="margin:4px 0 0;color:rgba(255,255,255,0.7);font-size:12px;">Plataforma Pública de Saúde</p>
          </td>
        </tr>
        <tr>
          <td style="padding:40px;">
            <p style="margin:0 0 8px;font-size:20px;font-weight:800;color:#0f172a;">Olá, {nome}!</p>
            <p style="margin:0 0 24px;font-size:15px;color:#64748b;line-height:1.6;">
              Recebemos uma solicitação de redefinição de senha para sua conta no
              <strong>MedBusca</strong>.<br><br>
              Clique no botão abaixo para criar uma nova senha.
            </p>
            <a href="{link}" style="display:inline-block;background:#2563eb;color:#fff;text-decoration:none;font-weight:700;font-size:15px;padding:16px 32px;border-radius:14px;">
              Redefinir minha senha
            </a>
            <p style="margin:24px 0 0;font-size:12px;color:#94a3b8;">
              Este link expira em <strong>1 hora</strong>. Se você não solicitou a redefinição, ignore este e-mail.
            </p>
          </td>
        </tr>
        <tr>
          <td style="background:#f8fafc;padding:20px 40px;border-top:1px solid #f1f5f9;">
            <p style="margin:0;font-size:11px;color:#cbd5e1;text-align:center;">
              MedBusca • Secretaria Municipal de Saúde
            </p>
          </td>
        </tr>
      </table>
    </td></tr>
  </table>
</body>
</html>
'''

    try:
        send_mail(
            subject='MedBusca — Redefinição de senha',
            message=f'Olá {nome}, clique no link para redefinir sua senha: {link}',
            from_email=settings.DEFAULT_FROM_EMAIL,
            recipient_list=[email],
            html_message=html,
            fail_silently=False,
        )
        return True
    except Exception as e:
        print(f'[EMAIL ERROR] {e}')
        return False
```

NÃO modificar a função enviar_email_ativacao existente.
  </action>
  <verify>
    <automated>cd /c/Users/anton/Downloads/medbusca && python -c "from api.email_service import enviar_email_redefinicao_senha; print('OK')"</automated>
  </verify>
  <done>Função enviar_email_redefinicao_senha importável sem erros. Aceita (nome, email, token) e retorna bool.</done>
</task>

<task type="auto">
  <name>Task 3: Implementar views esqueci_senha e redefinir_senha, registrar URLs</name>
  <files>api/views.py, api/urls.py</files>
  <action>
### api/views.py

1. Na linha 10, adicionar TokenRedefinicaoSenha ao import de models:
   ```python
   from .models import Alerta, Escala, Especialidade, Medico, Municipio, RegistroTurno, TokenAtivacao, TokenRedefinicaoSenha, Turno, UPA, Usuario
   ```

2. Após o bloco `# ─── Ativação de Conta ───` (após a view ativar_conta, antes de `# ─── Usuários ───`),
   adicionar o novo bloco:

```python
# ─── Recuperação de Senha ─────────────────────────────────────────────────────
@api_view(['POST'])
@permission_classes([AllowAny])
def esqueci_senha(request):
    """Gera token de redefinição e envia email. Resposta sempre 200 (não vaza existência do email)."""
    from .email_service import enviar_email_redefinicao_senha
    email = request.data.get('email', '').strip().lower()
    if not email:
        return Response({'erro': 'Email é obrigatório.'}, status=400)
    try:
        usuario = Usuario.objects.get(email=email, is_active=True)
    except Usuario.DoesNotExist:
        # Retorna 200 mesmo assim — não revela se o email existe
        return Response({'mensagem': 'Se este email estiver cadastrado, você receberá um link em breve.'})
    # Invalida tokens anteriores não usados do mesmo usuário
    TokenRedefinicaoSenha.objects.filter(usuario=usuario, usado=False).update(usado=True)
    token_str = secrets.token_urlsafe(48)
    TokenRedefinicaoSenha.objects.create(
        usuario=usuario,
        token=token_str,
        expira_em=timezone.now() + timedelta(hours=1),
    )
    enviar_email_redefinicao_senha(usuario.nome, usuario.email, token_str)
    return Response({'mensagem': 'Se este email estiver cadastrado, você receberá um link em breve.'})


@api_view(['POST'])
@permission_classes([AllowAny])
def redefinir_senha(request):
    token_str = request.data.get('token', '').strip()
    nova_senha = request.data.get('nova_senha', '')
    if not token_str or not nova_senha:
        return Response({'erro': 'Token e nova senha são obrigatórios.'}, status=400)
    if len(nova_senha) < 6:
        return Response({'erro': 'A senha deve ter pelo menos 6 caracteres.'}, status=400)
    try:
        t = TokenRedefinicaoSenha.objects.get(token=token_str, usado=False)
    except TokenRedefinicaoSenha.DoesNotExist:
        return Response({'erro': 'Link inválido ou já utilizado.'}, status=400)
    if timezone.now() > t.expira_em:
        return Response({'erro': 'Este link expirou. Solicite um novo.'}, status=400)
    t.usuario.set_password(nova_senha)
    t.usuario.save()
    t.usado = True
    t.save()
    return Response({'mensagem': 'Senha redefinida com sucesso! Você já pode fazer login.'})
```

As variáveis `secrets`, `timezone` e `timedelta` já estão importadas no topo de views.py
(linhas 1-3). NÃO duplicar imports.

### api/urls.py

Adicionar duas rotas no bloco `# Auth` (após `path('auth/me/', views.me)`):

```python
    path('auth/esqueci-senha/', views.esqueci_senha),
    path('auth/redefinir-senha/', views.redefinir_senha),
```

NÃO remover nem alterar nenhuma rota existente.
  </action>
  <verify>
    <automated>cd /c/Users/anton/Downloads/medbusca && python manage.py check --deploy 2>&1 | grep -E "ERROR|System check" && python -c "from api.views import esqueci_senha, redefinir_senha; print('views OK')" && python -c "from api.urls import urlpatterns; routes = [str(p.pattern) for p in urlpatterns]; assert any('esqueci' in r for r in routes); assert any('redefinir' in r for r in routes); print('urls OK')"</automated>
  </verify>
  <done>
    - Views esqueci_senha e redefinir_senha importáveis sem erros.
    - Rotas auth/esqueci-senha/ e auth/redefinir-senha/ presentes em urlpatterns.
    - python manage.py check não retorna ERROR.
    - POST /api/auth/esqueci-senha/ com email inválido retorna 400; com email inexistente retorna 200.
    - POST /api/auth/redefinir-senha/ com token inválido retorna 400.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anônimo -> POST /api/auth/esqueci-senha/ | Entrada não autenticada; pode ser abusada para enumeração de usuários ou flood de emails |
| Anônimo -> POST /api/auth/redefinir-senha/ | Token recebido via query param no link; exposição em logs de servidor se não tratado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | esqueci_senha | mitigate | Retornar sempre a mesma mensagem 200 independente de o email existir ou não (implementado na view) |
| T-01-02 | Denial of Service | esqueci_senha | accept | Sem rate limiting neste plano — Django não tem rate limit nativo; aceito para TCC; mitigar em produção com throttle de DRF ou middleware |
| T-01-03 | Elevation of Privilege | redefinir_senha | mitigate | Token de 64 bytes urlsafe (secrets.token_urlsafe(48)); single-use; expira em 1h; tokens anteriores invalidados em novo pedido |
| T-01-04 | Repudiation | TokenRedefinicaoSenha | accept | Modelo registra criado_em para auditoria básica; suficiente para TCC |
| T-01-05 | Spoofing | redefinir_senha | mitigate | Busca token com `usado=False` + cheque de expiração; token usado retorna 400 |
</threat_model>

<verification>
Após execução das 3 tasks, verificar end-to-end com o servidor local:

```bash
# Iniciar servidor (se não estiver rodando)
python manage.py runserver

# 1. Email inexistente — deve retornar 200 com mensagem genérica
curl -s -X POST http://localhost:8000/api/auth/esqueci-senha/ \
  -H "Content-Type: application/json" \
  -d '{"email": "naoexiste@example.com"}' | python -m json.tool

# 2. Email sem campo — deve retornar 400
curl -s -X POST http://localhost:8000/api/auth/esqueci-senha/ \
  -H "Content-Type: application/json" \
  -d '{}' | python -m json.tool

# 3. Token inválido — deve retornar 400
curl -s -X POST http://localhost:8000/api/auth/redefinir-senha/ \
  -H "Content-Type: application/json" \
  -d '{"token": "token-invalido", "nova_senha": "senha123"}' | python -m json.tool

# 4. Nova senha curta — deve retornar 400
curl -s -X POST http://localhost:8000/api/auth/redefinir-senha/ \
  -H "Content-Type: application/json" \
  -d '{"token": "qualquer", "nova_senha": "abc"}' | python -m json.tool
```

Para testar o fluxo completo (com email real), criar token manualmente no shell:
```bash
python manage.py shell -c "
import secrets
from django.utils import timezone
from datetime import timedelta
from api.models import Usuario, TokenRedefinicaoSenha
u = Usuario.objects.filter(is_active=True).first()
t = TokenRedefinicaoSenha.objects.create(usuario=u, token=secrets.token_urlsafe(48), expira_em=timezone.now()+timedelta(hours=1))
print('Token criado:', t.token)
"
# Usar o token impresso no POST para redefinir-senha
```
</verification>

<success_criteria>
1. `POST /api/auth/esqueci-senha/` com `{"email": "email@existente.com"}` retorna 200 e envia email (ou silent fail se email_service falhar).
2. `POST /api/auth/esqueci-senha/` com email não cadastrado retorna 200 com mesma mensagem (não vaza existência).
3. `POST /api/auth/redefinir-senha/` com token válido e `nova_senha` de 6+ chars altera a senha e marca o token como usado.
4. Segunda chamada com o mesmo token retorna 400 "Link inválido ou já utilizado."
5. Token com expira_em no passado retorna 400 "Este link expirou."
6. `python manage.py showmigrations api` mostra migration `tokens_redefinicao_senha` aplicada.
7. Nenhum endpoint existente quebrado (`python manage.py check` sem ERROR).
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-recupera-o-de-senha/01-01-SUMMARY.md` com:
- Arquivos modificados e o que foi adicionado em cada um
- Decisões tomadas (ex: ForeignKey vs OneToOne, query param reset_token vs token)
- Resultado de cada critério de sucesso
- Qualquer desvio do plano e motivo
</output>
